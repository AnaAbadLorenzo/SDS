language: java

jdk:
  - oraclejdk8

before_script:
  - export JAVA_HOME=/usr/lib/jvm/java-1.8.0-openjdk-amd64

install:
  - sudo apt-get install -y maven

script:
  - cd SDS/SDS-Controller
  - mvn clean install
